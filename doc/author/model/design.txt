  ┌────────────────────────────────────────────────────────────────────────────────┐
  │                           AUTHORIZATION MODEL LAYER                            │
  ├────────────────────────────────────────────────────────────────────────────────┤
  │  ┌─────────────────────────────────────────────────────────────────────────┐   │
  │  │                        USER IDENTITY MODELS                             │   │
  │  │   (Đại diện cho người dùng đã xác thực)                                 │   │
  │  │                                                                         │   │
  │  │   RbacUser (interface)                                                  │   │
  │  │       ├── RbacOAuth2User (record) ─── OAuth2User (Spring)               │   │
  │  │       ├── RbacOidcUser (record) ───── OidcUser (Spring)                 │   │
  │  │       └── RbacLdapUser (class) ────── UserDetails (Spring)              │   │
  │  │                    │                                                    │   │
  │  │                    ▼                                                    │   │
  │  │           AuthenticatedUser (record) ← Simplified extraction            │   │
  │  └─────────────────────────────────────────────────────────────────────────┘   │
  │  ┌─────────────────────────────────────────────────────────────────────────┐   │
  │  │                     RBAC CONFIGURATION MODELS                           │   │
  │  │   (Định nghĩa cấu trúc phân quyền - load từ YAML)                       │   │
  │  │                                                                         │   │
  │  │   Role ─────┬──── List<Subject> (ai được gán role)                      │   │
  │  │             ├──── List<String> clusters (áp dụng cho clusters nào)      │   │
  │  │             └──── List<Permission> (quyền của role)                     │   │
  │  │                          │                                              │   │
  │  │   DefaultRole ───────────┴──── List<Permission> (fallback)              │   │
  │  │                                      │                                  │   │
  │  │   Permission ────┬──── Resource (loại tài nguyên)                       │   │
  │  │                  ├──── List<PermissibleAction> (actions được phép)      │   │
  │  │                  └──── Pattern (regex match resource name)              │   │
  │  └─────────────────────────────────────────────────────────────────────────┘   │
  │  ┌─────────────────────────────────────────────────────────────────────────┐   │
  │  │                      ACCESS REQUEST MODELS                              │   │
  │  │   (Mô tả yêu cầu truy cập từ controller)                                │   │
  │  │                                                                         │   │
  │  │   AccessContext ────┬──── String cluster                                │   │
  │  │                     ├──── List<ResourceAccess> (resources cần access)   │   │
  │  │                     ├──── String operationName (for audit)              │   │
  │  │                     └──── Object operationParams (for audit)            │   │
  │  │                                  │                                      │   │
  │  │   SingleResourceAccess ──┬──── String name (resource identifier)        │   │
  │  │                          ├──── Resource resourceType                    │   │
  │  │                          └──── Collection<PermissibleAction>            │   │
  │  └─────────────────────────────────────────────────────────────────────────┘   │
  │  ┌─────────────────────────────────────────────────────────────────────────┐   │
  │  │                      PERMISSION ACTION MODELS                           │   │
  │  │   (Định nghĩa các hành động có thể thực hiện)                           │   │
  │  │                                                                         │   │
  │  │   PermissibleAction (sealed interface)                                  │   │
  │  │       ├── TopicAction (enum)                                            │   │
  │  │       ├── ConsumerGroupAction (enum)                                    │   │
  │  │       ├── SchemaAction (enum)                                           │   │
  │  │       ├── ConnectAction (enum)                                          │   │
  │  │       ├── AclAction (enum)                                              │   │
  │  │       ├── KsqlAction (enum)                                             │   │
  │  │       ├── ClusterConfigAction (enum)                                    │   │
  │  │       ├── ApplicationConfigAction (enum)                                │   │
  │  │       ├── AuditAction (enum)                                            │   │
  │  │       └── ClientQuotaAction (enum)                                      │   │
  │  │                                                                         │   │
  │  │   Resource (enum) ← Maps to corresponding Action enum                   │   │
  │  └─────────────────────────────────────────────────────────────────────────┘   │
  └────────────────────────────────────────────────────────────────────────────────┘
